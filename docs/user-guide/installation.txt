- if homebrew is not yet installed: /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
- install git: brew install git
- install pycharm professional
- access Github-Code:
# open https://github.com/XAI-Demonstrator/xai-demonstrator
# copy link under code button
# open terminal and navigate to desired folder
# execute: git clone „fill in github link“
- install node with following command
# brew install node@12
# OR: https://nodejs.org/dist/latest-v12.x/
- navigate in: visual-inspection/inspection-frontend/
- run following commands:
- npm install
- npm run serve